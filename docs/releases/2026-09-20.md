# September 20 release checkpoint

**Website and source published, silent local video rendered and final AWS experiment verified. Hosted CI and owner submission dependencies remain incomplete.** This document records completed checks separately from work in progress. It does not certify every browser, user workflow or reliability condition. Dates in this document are September 20, 2026 IST unless an original artifact provides UTC explicitly.

## What changed

The accepted order-and-receipts design is integrated into the local website sources and reproducible build. The scroll story advances the recorded delivery sequence and rewinds with backward scrolling; manual replay retains ownership until the user reconnects scroll. The compact and short-window layouts retain the same evidence semantics. Hover feedback remains.

The developer tutorial now starts with a supported passing case and then tests the more important bad repair: changing an order-ID key to SKU suppresses a legitimate order. The [actual local execution record](../repair-lab/key-scope-tutorial/README.md) retains reports, source snapshots, stdout/stderr and the archive hash. The new [form draft](../submission-form.md) includes the refreshed required profile, track, contribution and AWS-feedback fields without inventing owner information.

## Observed browser checks

These observations come from the current local review session. They are not anonymous verification of a deployed release.

| Check | Observed result | Scope |
| --- | --- | --- |
| Desktop layout | No horizontal document overflow observed | 1280×800 viewport |
| Phone layout | No horizontal document overflow observed | 390×844 viewport |
| Short narrow layout | No horizontal document overflow observed | 320×568 viewport |
| Keyboard candidate activation | Enter changed the candidate and focus stayed on its control | Reviewed candidate control journey; not full screen-reader certification |
| Fresh report imports | Before: passed, 1/1; SKU mutation: violation, 1/0; restored: passed, 1/1 | Actual reports from the clean-kit tutorial |
| Contradictory report | Rejected; prior report and downloadable bytes retained | Reviewed contradictory fixture |
| Invalid UTF-8 | Rejected; prior report and downloadable bytes retained | Reviewed malformed file |
| Original JSON download | Downloaded bytes matched the actual restored report exactly | Reviewed restored-report export |
| Scroll story | Forward and reverse progression passed; manual replay retained ownership | Reviewed scroll/manual interactions |
| Console | No warnings or errors observed | During the reviewed states only |

**Not executed:** a 200% zoom check and an operating-system reduced-motion preference test. Reduced-motion fallback exists in the source; this is not a claim that the preference was exercised at runtime. No comprehensive browser, assistive-technology or performance certification is claimed.

## Deployed website verification

Amplify **Deployment 4 succeeded**, checked at **2026-09-19 19:54:16.770 UTC**. The [hosting record](2026-09-20-hosting.json) identifies the 436,615-byte bundle, SHA-256 `0134ee4802fa4e25675946bd38901ac12fe8bb6e95cc5761d584895ee347b924`, and every staged asset. [Anonymous HTTPS verification](2026-09-20-anonymous-https.json) then matched the root and **all 37 staged assets** in 38 requests, with HTTP 200 responses and exact expected hashes.

The [initial anonymous check](2026-09-20-anonymous-initial-tls.json) remains preserved as incomplete after a local trust-store error. The successful rerun used the installed botocore CA bundle. Certificate and hostname verification remained enabled throughout; TLS verification was never disabled.

The [published-browser review](2026-09-20-browser.json), recorded at **2026-09-19 19:57:20.343 UTC**, confirmed the accepted root interface, order-key selection with focus retained, navigation to the AWS page, the exact final run ID/report hash, and the SKU terminal-rejection event. The AWS page showed the experiment passed with one business pass, two business violations and cleanup passed. Main and AWS pages had no observed document overflow at **390×844**; the AWS page's captured warning/error list was empty.

The published review did not execute 200% zoom, runtime reduced motion or completion of a new native browser kit-save operation. The public kit's HTTP bytes matched its staged ZIP. The earlier local original-JSON save was separately verified. These scopes remain distinct.

## Local execution and test checkpoint

The clean-kit tutorial used identical inputs, deliveries and faults across three real runs:

| Candidate source | Actual process exit | ORDER-A receipts | ORDER-B receipts | Derived result |
| --- | ---: | ---: | ---: | --- |
| Original order-ID key | 0 | 1 | 1 | Passed |
| Key changed to SKU | 1 | 1 | 0 | Violation |
| Exact original source restored | 0 | 1 | 1 | Passed |

Network access was denied by macOS `sandbox-exec`. The [summary](../repair-lab/key-scope-tutorial/summary.json) records equal cases/faults, source fingerprints, exact restoration and byte-identical kit re-export. This demonstrates the declared local receiver contract, not an actual external shipment or payment.

The earlier implementation checkpoint reported **131 Python tests and 105 JavaScript tests passing**. After the AWS verifier audit and targeted fixes below, the historical checkpoint was **144 SDK-free Python tests and 112 JavaScript tests passing**. Final [clean-source validation](2026-09-20-source-validation.json) passed **149 Python tests, 112 JavaScript tests, 45 independent-transfer checks and 19 release checks**, with network access denied. Five tests added after the 144-test checkpoint cover export boundaries and evidence preservation; AWS and demo artifacts were unchanged. These final results apply to source commit `0d0ec11ef4e3ba30ad03f2bd623f4e116d7ce664`, not later documentation-only updates. Deliberately bad candidates still produce business violations, and missing/unsupported controls remain incomplete/unresolved; a successful test suite does not mean every candidate is safe.

## AWS evidence

The preserved [September 18 AWS comparison](../validation.md#september-18-aws-comparison) passed 16 assertions and observed two unsafe receipts versus one repaired receipt. Its worker crashed after a successful fulfillment response. The recorded local harness instead interrupts the response after a simulated receipt commits. Those failure placements remain distinct.

**First new cloud attempt: unresolved.** Run `85500aff-3d08-483e-9e63-78a9723eb67a` sent six original messages. Our provider's JSON conflict logger could not serialize DynamoDB `Decimal` values from a boto3 receipt. `TypeError` prevented terminal rejection evidence for SKU-key ORDER-B; retries eventually reached the DLQ. The [preserved final report](../../evidence/key-scope-20260920.json) therefore has an unresolved experiment, with the SKU candidate incomplete. Its 2/2, 1/0 and 1/1 receipt counts did not turn that failed experiment into a pass.

The [first-attempt archive](../../web/aws-key-scope-attempt-1.zip), including captured source, has SHA-256 `ea41a00d1a9a753c8ea3165110ccfad657aebb21a2ad5b14214182d59cc775b4`. Keep this record and source distinct from any corrected rerun. The defect was found in our own lab, not by an outside user.

**Cleanup verified separately.** The original runner recorded cleanup as unconfirmed after its AWS error-metadata parser called `.get()` on `None`. A [later exact-stack-ARN read](../../evidence/key-scope-20260920-cleanup-followup.json) verified `DELETE_COMPLETE` at **2026-09-19 19:07:27 UTC**. This operational follow-up leaves the original report unchanged and does not change its business verdict.

**Second attempt: experiment passed; cleanup verified.** After Decimal normalization and optional-error handling fixes, run `366eeb3b-3258-474b-8136-792f1587ffa5` observed no-key 2/2 receipts (violation), SKU-key 1/0 with terminal rejection (violation), and order-key 1/1 (passed). The [raw second report](../../evidence/key-scope-20260920-r2.json) records experiment and operational status passed, with deletion verified at **2026-09-19 19:17:48 UTC**. The [second-attempt archive](../../web/aws-key-scope-attempt-2.zip) has SHA-256 `eb55556d4557f8b6bd6aa14c5f423d4b3c8579b638f7c08c95089e17ee618c5c` and retains its original source and verifier.

**Independent verifier audit and hardening.** Two deliberately tampered reports could pass the earlier checker: one omitted required source hashes, and another contained contradictory terminal events. The checker now rejects those cases; independent rechecking also confirmed that legitimate retries with distinct invocation IDs remain accepted. The second run's unchanged raw evidence still derives a pass under the strengthened checks. Its stored assertion list belongs to its original verifier version and differs from the newer list; preserve it rather than rewriting history. The audit concerns checker behavior on altered inputs, not a failed second AWS observation.

**Third attempt: passed with verified cleanup.** Run `82a1155f-c729-4b56-a32f-b4bc9a70a3ad` completed with **22/22 experiment checks passed** and **114 collector API calls**. These are checks on one declared experiment, not 22 scenarios. The [final raw report](../../evidence/key-scope-20260920-r3.json) was recorded at **2026-09-19 19:36:17.792 UTC**. It records verified deletion at **19:36:17.789 UTC**, with no cleanup errors. Cleanup used a separate 31 API calls; the 114 figure describes collection only.

| Final AWS candidate | ORDER-A receipts | ORDER-B receipts | Business result |
| --- | ---: | ---: | --- |
| No key | 2 | 2 | Violation: duplicates |
| SKU key | 1 | 0 | Violation: valid order rejected |
| Order key | 1 | 1 | Passed for this run |

The experiment passing means the expected two bad repairs were detected and the order-key candidate passed the declared checks. It does not turn the faulty candidates green or establish every possible retry schedule. Worker/provider behavior was unchanged from the second run; this fresh record captures the hardened verifier SHA-256 `a3cc1a2612fdadfe88a04a84825e199040c10becf4a4ff4913a94ec9ed361dbc` and its generated assertion list together. [Engineering learning](../what-we-learned.md#test-the-verifier-with-bad-evidence).

The local UI is built with an exact copy of that final report. Release artifacts are:

| Artifact | SHA-256 |
| --- | --- |
| [Report copied into the site](../../web/aws-key-scope-report.json), byte-identical to the raw report | `49132ede7271bfa8c1e091bbde339a350eece3d47f8a41ce17af9fce4047d4f2` |
| [Final AWS regression archive](../../web/aws-key-scope-regression.zip) with captured sources | `e097d77241354bab95453a8610f00366e26e24bba35531139ebaedee34fe8ff4` |

These exact files are now published in Deployment 4 and passed the [anonymous asset verification](2026-09-20-anonymous-https.json). The first two attempts and their original archives remain preserved and were also checked in that published release.

The public viewer remains a static interface; the experimental run is controlled separately. The $25 gross project ceiling includes a $5 reserve. Credits, expiry, mapping concurrency and billing alerts are not hard dollar caps.

## Media and publication

The completed silent local delivery is `outputs/replayguard-demo-silent.mp4` in the working workspace. It is **154.000 seconds**, **1920×1080 H.264 at 30 fps**, with **4,620 video frames and zero audio streams**. File size is **9,472,219 bytes**, SHA-256 `bf6af0ae5585a044ce58eae3dafedc64374ddd86362710fdf6d7d8f8f79f120c`. Full decode and all **14 technical checks passed**; the [portable technical record](2026-09-20-silent-video.json) retains the checks and source identities. The final duration supersedes the earlier 164-second edit plan.

The film composes timestamped screenshot sequences from actual browser interactions and recorded code-execution material with explanatory animation and documented reading holds/omitted elapsed gaps. Its 30 fps output is not a claim of a continuous native 30 fps screencast or the real duration of the executions. The completed [visual review](2026-09-20-silent-video-review.json) inspected **28 actual encoded frame samples covering every scene**, with no blocker found within that sample; this is not a full real-time viewing claim. The user prohibited a new YouTube upload and new narration; no such actions were performed.

The [frozen video source verification](../../demo/silent-20260920/source-verification.json) covers a static source package of **174 files / 8,019,858 bytes**. All **16 capture clips rebuilt byte-identically** using the same installed locked renderer runtime, and the prepared composition matched. A second complete master render and a fresh npm installation were **not performed**. This is the demonstrated reproduction scope; the final MP4 is delivered separately from the static source package.

The existing [YouTube film](https://youtu.be/CGE19upS66A) remains the September 19 unlisted 2:43 video. Its original narration, hash and validation are retained in the [historical publication record](../publication-status.md#september-19-publication-record). It shows the earlier UI and key-removal/restoration tutorial. It must not be relabelled as the new silent film or used to imply that it shows this release's added interface or AWS behavior. A local MP4 does not replace the event's required YouTube link.

The accepted interface and current AWS viewer are live at the [Amplify URL](https://prod.d2w687q4ucx6dk.amplifyapp.com/) through verified Deployment 4. Source commit [`0d0ec11ef4e3ba30ad03f2bd623f4e116d7ce664`](https://github.com/sudo-anshul/replayguard/commit/0d0ec11ef4e3ba30ad03f2bd623f4e116d7ce664) is **published**. [Anonymous pinned-commit checks](2026-09-20-github-public.json) returned HTTP 200 and exact hashes for the README, hardened AWS verifier and frozen demo manifest.

The local companion source ZIP contains **679 files / 18,950,557 bytes**, SHA-256 `c100abac4a3361b8f153594de83a68373ae207a701ffb040d9f5207ceec85859`. Its [clean-extraction record](2026-09-20-source-validation.json) confirms that 676 repository source files match the published commit, checksums and source manifests match, the tested extraction stayed unchanged, and deterministic re-export produced identical ZIP bytes. The ZIP remains a **local companion artifact**, not a publicly uploaded release asset. The silent MP4 is separate. Subsequent documentation-only changes record these results; they are not the tested source commit or part of that frozen ZIP.

Hosted [CI run 35466519733](https://github.com/sudo-anshul/replayguard/actions/runs/35466519733) at that exact source commit is **incomplete**: GitHub reported failure but executed no job steps. Its [record](2026-09-20-github-ci.json) preserves the exact annotation: “The job was not started because your account is locked due to a billing issue.” This is neither a hosted test failure nor a hosted pass; no account or billing settings were changed.

## Remaining evidence and owner dependencies

- Preserve all three AWS attempts and their source versions. The third run/cleanup and the published report/archive byte checks are complete; earlier records remain unchanged.
- Retain the completed silent local video's metadata and portable validation records with the release. Make no new YouTube publication claim.
- Keep Deployment 4's hosting, exact-byte and scoped browser records with the release; retain the unexecuted zoom/reduced-motion/native kit-save limitations.
- Hosted CI remains incomplete: [run 35466519733 at the published source commit](2026-09-20-github-ci.json) executed no steps because of the account billing block. Local checks do not establish a hosted pass.
- No [outside-developer trial](../first-user-trial.md) has been conducted. Synthetic transfer and agent review do not establish adoption.
- Confirm missing owner/team profiles and contributions, registration/check-in and applicable student status; record actual event-submission confirmation. None has been filled by assumption.

Use the earliest observed cutoff, **September 20 at 09:00 IST**, with a 07:30 submission target. A [fresh API read at 2026-09-19 19:48:16 UTC](2026-09-20-deadline.json) confirmed that time; it does not resolve the earlier countdown conflict. A separate [cloud postflight at the same time](2026-09-20-cloud-postflight.json) confirmed all three exact experimental stack ARNs at `DELETE_COMPLETE`, with the hosting stack retained. [Earlier deadline evidence](../deadline.md) and [submission package](../submission.md).
